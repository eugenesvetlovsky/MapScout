# Infrastructure

## Start

```bash
cd infra
docker compose up --build
```

## Services

- `web` - React app on `5173`.
- `api` - FastAPI on `8000`.
- `postgres` - PostgreSQL on `5432`.
- `redis` - Redis on `6379`.
